
import { precioElement } from './cart.js';

/*
(() => {
 const precio = JSON.stringify(localStorage.getItem("precio"));
 precioSubtotal.value = precio;

 const iva = precio * 0.12;

 const total = precio + iva;

})();*/

/*
function calcularPrecioTotal (){
    precioElement = JSON.parse(localStorage.getItem("precio"));
    precioSubtotal.innerHTML = precioElement;
    const iva = precio * 0.12;
    const total = precio + iva;
}

calcularPrecioTotal();

*/

